# node-linker=hoisted: flat node_modules — Next.js ưa layout này trong monorepo pnpm.
node-linker=hoisted
# Bật script của dependency cần build (esbuild, sharp...) khi cài.
enable-pre-post-scripts=true
