# node-linker=hoisted: flat node_modules — Expo/React Native (Metro) và Next.js đều ưa layout này
# trong monorepo pnpm; tránh lỗi resolve native module.
node-linker=hoisted
# Bật script của dependency cần build (esbuild, sharp...) khi cài.
enable-pre-post-scripts=true
